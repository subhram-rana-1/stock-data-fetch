smooth_price_averaging_method = 'simple'  # exponential / simple
smooth_price_period = 150
smooth_price_ema_period = int(smooth_price_period * 30)
# smooth_price_ema_period = 10

smooth_slope_averaging_method = 'exponential'  # exponential / simple
smooth_slope_period = 10
smooth_slope_ema_period = 10

smooth_momentum_averaging_method = 'simple'  # exponential / simple
smooth_momentum_period = 10
smooth_momentum_ema_period = 30
